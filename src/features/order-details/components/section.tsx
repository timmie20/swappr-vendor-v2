// Promoted to a shared component once product-details needed the same
// primitives — re-exported here so existing imports keep working.
export { Section, InfoRow } from "@/components/shared/section";
